- user ||= current_user
- user = current_user unless current_user.admin?
- collection ||= false

- form_for current_user, :html => {:class => 'benchform'} do |f|
  %ul{:id => "dropzone_bench", :class => "benchlist catcher", :catches => "#{Kobble.organised_models.join(',')}"}
    %h3
      - if collection
        = collection.abbreviation
      - else
        combined
      workbench
    = render :partial => 'users/marker', :collection => collection ? user.current_markers : user.markers
    %p.explanatory
      Click on a listing to select it. Shift- or cmd-click to select several. Actions will be offered. Drag up and down to sort this list. Drag out into space to remove. Drag onto something else to apply.
