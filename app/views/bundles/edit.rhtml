<h1>Edit <%= link_to @bundle.name, :action => 'show', :id => @bundle.id %></h1>

<% form_for :bundle,
            :url => bundle_path(@bundle),
            :html => { 
              :method => "put",
              :multipart => true
            } do |f| 
%>
  <%= render :partial => 'form' %>
<% end %>

