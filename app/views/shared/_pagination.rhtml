<% if list %>
  <div class="pagination">
    found: <span class="current"><%= pluralize list.total_entries, @klass ? @klass.nice_title : 'item' %></span> in <%= link_to pluralize( current_collections.size, 'collection'), :controller => 'collections', :action => 'list' %><br />
    show as: <%= render :partial => 'shared/viewcontrol' %><br />
    pages: <%= will_paginate list, :prev_label => 'previous', :next_label => 'next', :separator => ' &middot; ', :container => false %>
  </div>
<% end %>