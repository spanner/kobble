<h1>
	<% if @node.source %>Excerpt a new snippet from 
		<%= link_to @node.source.name, {:controller => 'sources', :action => 'show', :id => @node.source} %>
	<% else %>
		Create a new fragment
	<% end %>
</h1>

<%= form_tag( {:action => 'create'}, :id => 'nodeform', :multipart => true) %>
 <%= render :partial => 'form' %>
<%= end_form_tag %>
